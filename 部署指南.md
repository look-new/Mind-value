# 🚀 MindVault 部署指南

本指南将帮助您将 MindVault 项目部署到 GitHub 和 Vercel。

---

## 📋 代码检查结果

### ✅ 代码质量
- **无 Linter 错误** - 代码通过 ESLint 检查
- **TypeScript 配置正确** - 类型检查通过
- **项目结构清晰** - 组件、服务、类型定义分离良好
- **依赖管理完善** - package.json 配置正确

### ✅ 已配置的文件
- ✅ `.gitignore` - 已正确配置，排除 node_modules、dist 等
- ✅ `vercel.json` - Vercel 部署配置已就绪
- ✅ `.github/workflows/deploy.yml` - GitHub Actions 工作流已配置
- ✅ `vite.config.ts` - 已优化支持 GitHub Pages 和 Vercel

### ⚠️ 注意事项
1. **环境变量**: 如需使用 DeepSeek AI 功能，需要配置 `VITE_DEEPSEEK_API_KEY`
2. **数据存储**: 数据保存在浏览器 LocalStorage，清除浏览器数据会丢失
3. **网络依赖**: 需要访问 Tailwind CSS CDN 和 Google Fonts

---

## 📦 部署到 GitHub

### 步骤 1: 创建 GitHub 仓库

1. 登录 [GitHub](https://github.com)
2. 点击右上角 **+** → **New repository**
3. 填写仓库信息：
   - **Repository name**: `mind-vault` (或您喜欢的名称)
   - **Description**: 个人知识库应用
   - **Visibility**: Public 或 Private
   - **不要**勾选 "Initialize with README"（因为您已有代码）
4. 点击 **Create repository**

### 步骤 2: 初始化 Git 并推送代码

在项目根目录打开终端，执行以下命令：

```bash
# 初始化 Git 仓库（如果还没有）
git init

# 添加所有文件
git add .

# 提交代码
git commit -m "Initial commit: MindVault 知识库应用"

# 添加远程仓库（替换 YOUR_USERNAME 和 REPO_NAME）
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 步骤 3: 启用 GitHub Pages

1. 进入您的 GitHub 仓库
2. 点击 **Settings** → **Pages**
3. 在 **Source** 部分：
   - 选择 **Source**: `GitHub Actions`
4. 点击 **Save**

### 步骤 4: 配置 DeepSeek API Key（可选）

如果您想使用 AI 摘要功能：

1. 进入仓库 → **Settings** → **Secrets and variables** → **Actions**
2. 点击 **New repository secret**
3. 添加：
   - **Name**: `VITE_DEEPSEEK_API_KEY`
   - **Value**: 您的 DeepSeek API Key（获取地址：https://platform.deepseek.com/）
4. 点击 **Add secret**

> **注意**: 不配置 API Key 也能运行，只是 AI 功能会使用默认摘要。

### 步骤 5: 等待自动部署

- 推送代码后，GitHub Actions 会自动触发部署
- 查看部署状态：进入仓库 → **Actions** 标签页
- 部署完成后，访问：`https://YOUR_USERNAME.github.io/REPO_NAME/`

---

## 🌐 部署到 Vercel

### 方法一：通过 GitHub 导入（推荐）

1. **登录 Vercel**
   - 访问 [vercel.com](https://vercel.com)
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 **Add New...** → **Project**
   - 选择您的 GitHub 仓库 `mind-vault`
   - 点击 **Import**

3. **配置项目**
   - Vercel 会自动检测到 Vite 项目
   - 确认以下配置：
     - **Framework Preset**: Vite
     - **Build Command**: `npm run build`
     - **Output Directory**: `dist`
     - **Install Command**: `npm install`
   - 点击 **Deploy**

4. **配置环境变量（可选）**
   - 如果需要 AI 功能，在项目设置中添加：
     - **Name**: `VITE_DEEPSEEK_API_KEY`
     - **Value**: 您的 DeepSeek API Key
   - 路径：**Settings** → **Environment Variables**

5. **等待部署完成**
   - Vercel 会自动构建并部署
   - 部署完成后会提供访问链接，例如：`https://mind-vault.vercel.app`

### 方法二：使用 Vercel CLI

```bash
# 安装 Vercel CLI
npm i -g vercel

# 在项目根目录执行
vercel

# 按照提示操作：
# - 登录 Vercel 账号
# - 选择项目设置
# - 确认部署
```

---

## 🔧 验证部署

部署成功后，检查以下功能：

- ✅ 页面正常显示
- ✅ 可以添加资源
- ✅ 搜索和筛选功能正常
- ✅ 数据保存在浏览器 LocalStorage
- ✅ 导出/导入 JSON 功能正常
- ✅ AI 摘要功能（如果配置了 API Key）

---

## 🐛 常见问题

### GitHub Pages 问题

**Q: 页面显示空白或 404？**
- 检查 GitHub Actions 部署是否成功
- 确认 GitHub Pages 设置中 Source 选择了 "GitHub Actions"
- 检查仓库名称是否正确（URL 格式：`https://username.github.io/repo-name/`）

**Q: 样式丢失？**
- 检查网络连接（需要访问 Tailwind CSS CDN）
- 查看浏览器控制台是否有错误

### Vercel 问题

**Q: 部署后出现 404 错误？**
- 确认 `vercel.json` 文件存在且配置正确
- 检查 Vercel 项目设置中的 Output Directory 是否为 `dist`
- 确认 Build Command 为 `npm run build`

**Q: 构建失败？**
- 查看 Vercel 构建日志
- 检查 TypeScript 错误：`npm run build` 本地测试
- 确认所有依赖都已安装

**Q: 环境变量不生效？**
- 确认环境变量名称正确：`VITE_DEEPSEEK_API_KEY`
- 在 Vercel 中重新部署（环境变量更改后需要重新部署）

---

## 📝 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

### 本地配置环境变量

创建 `.env.local` 文件（已添加到 .gitignore）：

```env
VITE_DEEPSEEK_API_KEY=your_api_key_here
```

---

## 🔄 更新部署

### GitHub Pages
- 推送代码到 `main` 或 `master` 分支
- GitHub Actions 会自动重新部署

### Vercel
- 推送代码到连接的 GitHub 分支
- Vercel 会自动检测并重新部署
- 或手动触发：Vercel Dashboard → 项目 → **Redeploy**

---

## 📚 相关文档

- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Vercel 文档](https://vercel.com/docs)
- [Vite 文档](https://vitejs.dev/)
- [React 文档](https://react.dev/)

---

## ✅ 部署检查清单

### GitHub Pages
- [ ] 代码已推送到 GitHub
- [ ] GitHub Pages 已启用（Source: GitHub Actions）
- [ ] GitHub Actions 部署成功
- [ ] 可以访问网站
- [ ] （可选）DeepSeek API Key 已配置

### Vercel
- [ ] 项目已导入到 Vercel
- [ ] 构建配置正确
- [ ] 部署成功
- [ ] 可以访问网站
- [ ] （可选）环境变量已配置

---

祝您部署顺利！🎉

